version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://echo:echo@postgres:5432/echoshield
      - REDIS_URL=redis://redis:6379
      - MODEL_DIR=/app/models
      - ASR_MODEL_ID=openai/whisper-large-v3
      - SCAMBERT_MODEL_PATH=/app/models/scambert_final
      - DEEPFAKE_MODEL_PATH=/app/models/lightcnn9_audio.pth
      - CHROMA_PERSIST_DIR=/app/data/embeddings/chroma
    volumes:
      - ./models:/app/models
      - ./data:/app/data
      - model_cache:/app/models/cache
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4

  postgres:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: echoshield
      POSTGRES_USER: echo
      POSTGRES_PASSWORD: echo
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U echo"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  celery:
    build:
      context: .
      dockerfile: Dockerfile
    command: celery -A app.celery worker --loglevel=info --concurrency=4
    environment:
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgresql://echo:echo@postgres:5432/echoshield
    depends_on:
      - redis
      - postgres
    volumes:
      - ./data:/app/data

  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./../monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana

volumes:
  pgdata:
  redisdata:
  grafana_data:
  model_cache: